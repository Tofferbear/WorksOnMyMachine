// src/managers/MilitaryOperationsManager.ts

import { SpawnManager } from "./SpawnManager";

export class MilitaryOperationsManager {

  public run(): void {
      // High-level military planning (raids, sieges) will go here.
      // Reactive defense and garrisons are now handled autonomously by modular MooseRole.
  }

}
